import { test } from "tap";
import { sendTouches as _sendTouches } from "../../src/grpc/send-touch.js";

test("TODO: test touch events for the emulator somehow", { skip: true }, async (t) => {
    t.fail();
});
